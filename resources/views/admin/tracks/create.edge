@layout('layouts/admin')

@set('title', t('admin.track.title.create'))

@section('body')
  <div class="container px-6 mx-auto">
    @!title({ text: t('admin.track.title.create') })

    <div
      class="px-4 py-3 mb-8 bg-white rounded-lg shadow-md dark:bg-gray-800"
    >
      <form action="{{ route('admin_tracks.store') }}" method="POST" enctype="multipart/form-data">
        @include('admin/tracks/_form')
      </form>
    </div>
  </div>
@end
